name: push-probe

"on":
  push:
    branches:
      - main

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Prove push triggers Actions
        run: echo "push trigger ok sha=${GITHUB_SHA}"
